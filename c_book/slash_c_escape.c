#include <stdio.h>

int main() {
    printf("\dls\n");
    return 0;
}

// Exercise 1-2
// Outcome: attempting to use a non-standard escape character generates the error:
// "Unknown escape sequence"
// In the print output, the slash is omitted and the letter is printed.
